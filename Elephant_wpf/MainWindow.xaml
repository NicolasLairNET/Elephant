<Window x:Class="Elephant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elephant_wpf"
        mc:Ignorable="d"
        Title="Elephant" Height="600" Width="800" ResizeMode="CanResize" MinHeight="400" MinWidth="400">
    <Grid Background="#FF174489">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="1">

            <StackPanel Orientation="Horizontal">
                <Border  BorderBrush="White"  Height="34" Margin="5,20,5,10" Width="150" CornerRadius="6" Background="White">
                    <TextBox x:Name="txtName" Width="225" Height="34" FontSize="20" BorderBrush="{x:Null}" Background="Transparent" VerticalAlignment="Center"/>
                </Border>

                <Border  BorderBrush="White" Height="34" Width="45" Margin="0,20,0,10" CornerRadius="6" Background="White">
                    <Button x:Name="btnSearch" Width="auto" Click="SearchTag_Click" Height="auto" Background="Transparent" FontFamily="Calibri Light" BorderBrush="{x:Null}">
                        <Image Margin="0"  Height="auto" Width="auto" Source="/loupe.jpg"/>
                    </Button>
                </Border>
            </StackPanel>

            <Border  BorderBrush="White" Height="34" Width="200" Margin="5,0,0,0" CornerRadius="6" Background="White">
                <Button x:Name="btnUpdateData" Click="BtnUpdateData_Click" Background="Transparent" FontFamily="Calibri Light" BorderBrush="{x:Null}">Mise à jour des données</Button>
            </Border>
        </StackPanel>

        

        <DataGrid Grid.Row="1" Grid.ColumnSpan="3" AutoGenerateColumns="False" Height="auto" Margin="20" x:Name="DGridResult" ItemsSource="{Binding TagsList}" IsReadOnly="True" ClipboardCopyMode="IncludeHeader">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Point" Width="*" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Paramètres" Width="*" Binding="{Binding Parameter}"/>
                <DataGridTextColumn Header="Valeur" Width="*" Binding="{Binding Value}"/>
                <DataGridTextColumn Header="Origine" Width="*" Binding="{Binding Origin}"/>
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Window>
